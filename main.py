def add_numbers(a: int, b: int) -> int:
    """Пример функции для тестирования."""
    return a + b

if __name__ == "__main__":
    # Пример использования (для проверки вручную)
    print("Результат:", add_numbers(2, 3))